# setup.py

from setuptools import setup, find_packages


setup(
    name='django_beta',
    version='0.0.1-alpha',
    url='',
    description='An easy way to capture beta signups on a django site.',
    author='Greg Aker',
    author_email='me@gregaker.net',
    packages=find_packages(),
    classifiers=[],
)
